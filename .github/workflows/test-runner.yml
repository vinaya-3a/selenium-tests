name: Run Specific TestNG Test Cases

on:
  workflow_dispatch:
    inputs:
      testcases:
        description: 'Test Cases to Run'
        required: true
        default: 'RunAll'

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'  # ← Add this line
          java-version: '17'
          java-package: jdk
          check-latest: false
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: ${{ secrets.GITHUB_TOKEN }}
          overwrite-settings: true

      - name: Run tests
      run: |
        if [ "${{ github.event.inputs.testcase }}" == "RunAll" ]; then
          mvn test
        else
          mvn test -Dtest=TestSuite#${{ github.event.inputs.testcase }}
        fi